1) ќтсортируйте данный массив, использу€ встроенную сортировку.
ѕерва€ строка входных данных содержит количество элементов в списке N. ƒалее идет N целых чисел, выведите эти числа в пор€дке неубывани€ (это как по возрастанию, термин "неубывание" используетс€ чтобы избежать неоднозначного понимани€ в случае наличи€ одинаковых чисел в списке).

N=int(input())
lst=['0']
for i in range(0,N):
    a=str(input())
    lst=lst+[a]
sortedlst=sorted(lst)

print('\n'.join(sortedlst[1::]))



2) ¬ первой строке задаЄтс€ количество названий столиц - число N. ¬ следующих N строках задаютс€ названи€ столиц по одному в строке. ќтсортируйте названи€ столиц в алфавитном пор€дке и выведите их по одному в строке.

def mykey(x):
    return x[1]

N=int(input())
lst=['0']
for i in range(0,N):
    a=str(input()).split()
    lst=lst+[a]
sortedlst=sorted(lst, key=mykey)

print('\n'.join(sortedlst[1::]))



3) ¬ первой строке задаЄтс€ количество названий столиц и государств - число N. ¬ следующих N строках задаютс€ названи€ столиц и государств по одному в строке, слова раздел€ютс€ одним пробелом. ќтсортируйте названи€ столиц и государств по названию государства в алфавитном пор€дке и выведите их по одному в строке.
def mykey(x):
    return x[1]

N=int(input())
lst=tuple()
for i in range(0,N):
    a=str(input()).split()
    lst=lst+(a,)
sortedlst=sorted(lst, key=mykey)

for i in range(0,N):
    sortedlst[i]=' '.join(sortedlst[i])
    print(sortedlst[i])



4) ƒан список чисел. ќпределите, сколько в нем встречаетс€ различных чисел. Ќе забудьте, что функци€ len подходит и дл€ множеств.

myset=set(map(int, input().split()))
set(myset) - убираем повтор€ющиес€ элементы
n=len(myset)
print(n)  



5) ƒаны два списка чисел. ѕосчитайте, сколько чисел содержитс€ одновременно как в первом списке, так и во втором.

myset1=set(map(int, input().split()))
myset2=set(map(int, input().split()))

myset1=set(myset1)
myset2=set(myset2)
nall=len(myset1)+len(myset2) 

setsum=myset1|myset2
setsum=set(setsum) 

k=nall-len(setsum)
print(k)





6) ¬о входной строке записана последовательность чисел через пробел. ƒл€ каждого числа выведите слово YES (в отдельной строке), если это число ранее встречалось в последовательности или NO, если не встречалось.

myset=list(map(int, input().split()))
a=set()

for i in range(0,len(myset)):
        if myset[i] in a:
            print('YES')
        else:
            a.add(myset[i])
            print('NO')
